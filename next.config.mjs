/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'crystalmedia-backend.onrender.com', 'crystalmedia-be.up.railway.app', 'res.cloudinary.com'],
  },
};

export default nextConfig;
